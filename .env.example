# Copy this to .env and fill in your Claude API key for local development.
# Do NOT commit your .env file. The repo .gitignore already ignores it.
#
# To use:
# 1. cp .env.example .env
# 2. Edit .env and paste your Claude API key after the =
# 3. pip install python-dotenv requests
# 4. python scripts/use_claude.py

@"
CLAUDE_API_KEY=REDACTED_API_KEY
"@ | Out-File -FilePath .env -Encoding utf8
python scripts/use_claude.py